import Slider from '@mui/material/Slider';

export const PlayBar = ({ playbackState }) => {
    return (
        <div style={{position: 'absolute', top: '90%', width: "100%", height: "10%", backgroundColor: 'red'}}>
        </div>
    )
}